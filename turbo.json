{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": [
    "HOST",
    "PORT",
    "VITE_WS_URL",
    "VITE_DEV_SERVER_URL",
    "NUECODE_LOG_WS_EVENTS",
    "NUECODE_MODE",
    "NUECODE_PORT",
    "NUECODE_NO_BROWSER",
    "NUECODE_HOME",
    "NUECODE_AUTH_TOKEN",
    "NUECODE_DESKTOP_WS_URL",
    "NUECODE_TRACE_MIN_LEVEL",
    "NUECODE_TRACE_TIMING_ENABLED",
    "NUECODE_TRACE_FILE",
    "NUECODE_TRACE_MAX_BYTES",
    "NUECODE_TRACE_MAX_FILES",
    "NUECODE_TRACE_BATCH_WINDOW_MS",
    "NUECODE_OTLP_TRACES_URL",
    "NUECODE_OTLP_METRICS_URL",
    "NUECODE_OTLP_EXPORT_INTERVAL_MS",
    "NUECODE_OTLP_SERVICE_NAME"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "dist-electron/**"]
    },
    "dev": {
      "dependsOn": ["@t3tools/contracts#build"],
      "cache": false,
      "persistent": true
    },
    "typecheck": {
      "dependsOn": ["^typecheck"],
      "outputs": [],
      "cache": false
    },
    "test": {
      "dependsOn": ["^build"],
      "cache": false,
      "outputs": []
    }
  }
}
